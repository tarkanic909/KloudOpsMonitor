version: "3.9"

services:
  backend:
    image: ghcr.io/tarkanic909/kloudops-backend:${VERSION}
    env_file:
      - .env.prod
    restart: always

  agent:
    image: ghcr.io/tarkanic909/kloudops-agent:${VERSION}
    env_file:
      - .env.prod
    restart: always

